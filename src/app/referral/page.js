import Referral from "./Referral";

export const metadata = {
    title: "test referral page | beclean",
    description: "referral page description",
};

const ReferralWrapper = () => {
  return (
    <main>
        <Referral/>
    </main>
  )
}

export default ReferralWrapper